# vspec-python

Python bridge for Vspec Engine Week 9-10:

- `runtime_bridge`: ctypes bridge to `vspec_engine_capi` shared library
- `torch_like_api`: `load()` and `generate()` usage pattern
- `cli`: converter wrapper command (`vspec-convert`)
